// Overview page component, site/webapp/view/hello-vue/templates/overview.tmpl
/**
 * @name            jPulse Framework / Site / WebApp / View / Hello Vue / Templates / Overview Page
 * @tagline         Overview of Hello Vue Single Page Application
 * @description     This page gives an overview of the Vue.js SPA
 * @file            site/webapp/view/hello-vue/templates/overview.tmpl
 * @version         1.4.18
 * @release         2026-01-24
 * @repository      https://github.com/jpulse-net/jpulse-framework
 * @author          Peter Thoeny, https://twiki.org & https://github.com/peterthoeny/
 * @copyright       2025 Peter Thoeny, https://twiki.org & https://github.com/peterthoeny/
 * @license         BSL 1.1 -- see LICENSE file; for commercial use: team@jpulse.net
 * @genai           60%, Cursor 1.7, Claude Sonnet 4
 */

window.jpSpaRoutes.Overview = {
    template: `
        <div class="jp-info-box">
            <h3>🚀 Vue.js SPA Demo</h3>
            <p>This is a Single Page Application (SPA) built with Vue.js and Vue Router.</p>
            <ol class="jp-list">
                <li>Notice how navigation changes the URL, but the page doesn't reload - just like Gmail!</li>
                <li>Try adding a to-do item to see Vue reactivity in action</li>
                <li>Check your browser's Network tab to see the API calls</li>
                <li>Study the code examples to learn how this is implemented</li>
                <li>Copy the <tt>site/webapp/view/hello-vue/</tt> directory, and use it as a template for your own SPA</li>
            </ol>
            <p><strong>Educational Goal:</strong> Learn how to build modern SPAs with Vue.js and integrate them with jPulse Framework backends.</p>
        </div>

        <div class="jp-dashboard-grid local-dashboard-grid">
            <button v-for="route in routes"
                    :key="route.path"
                    @click="navigate(route.path)"
                    class="jp-card-dashboard local-clickable-card">
                <div class="jp-icon-container local-icon-container">{{ route.icon }}</div>
                <h3 class="jp-card-title">{{ route.title }}</h3>
                <p class="jp-card-description">{{ route.description }}</p>
            </button>
        </div>
    `,
    data() {
        return {
            availableRoutes: []
        };
    },
    computed: {
        routes() {
            const filtered = (this.availableRoutes || []).filter(r => {
                return r && r.path && r.path !== '/overview';
            });
            return filtered;
        }
    },
    methods: {
        navigate(path) {
            if (this.$router) {
                this.$router.push(path);
            } else {
                console.warn('- Router not available yet');
            }
        }
    },
    mounted() {
        this.availableRoutes = window.helloVueRoutes || [];
    }
};

// EOF Overview page component, site/webapp/view/hello-vue/templates/overview.tmpl
