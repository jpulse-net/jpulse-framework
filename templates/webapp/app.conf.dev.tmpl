/**
 * @name            jPulse Framework / Site / WebApp / Development Deployment Configuration
 * @tagline         Template for development deployment configuration
 * @description     Minimal configuration for local development deployment
 * @file            site/webapp/app.conf
 * @version         1.6.6
 * @release         2026-02-03
 * @repository      https://github.com/jpulse-net/jpulse-framework
 * @author          Site Administrator
 * @copyright       2025 Site Administrator
 * @license         BSL 1.1 -- see LICENSE file; for commercial use: team@jpulse.net
 * @genai           60%, Cursor 1.7, Claude Sonnet 4
 */

// Development deployment configuration
// Generated by jpulse-setup for local development
{
    app: {
        site: {
            name: '%SITE_NAME%',
            shortName: '%SITE_SHORT_NAME%'
        },
        siteId: '%JPULSE_SITE_ID%',
        domainName: '%JPULSE_DOMAIN_NAME%'
    },

    deployment: {
        mode: 'dev',
        dev: {
            name:       'development',
            db:         '%DB_NAME%',
            port:       %PORT%
        }
    },

    middleware: {
        session: {
            secret: '%SESSION_SECRET%',
            cookie: {
                secure: false  // HTTP OK for development
            }
        }
    },

    database: {
        mode: 'standalone',
        standalone: {
            // Uses framework defaults with deployment.dev.db name
            // No authentication needed for development
        }
    },

    // Redis configuration (single mode for dev PM2 clusters)
    redis: {
        enabled:    true,
        mode:       'single',
        single: {
            host:       '%REDIS_HOST%',
            port:       %REDIS_PORT%,
            password:   '%REDIS_PASSWORD%',
            db:         0
        }
    }

    // Uses framework defaults for logging, etc.
}

// EOF site/webapp/app.conf
