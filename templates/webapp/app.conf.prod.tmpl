/**
 * @name            jPulse Framework / WebApp / Production Deployment Configuration
 * @tagline         Template for production deployment configuration
 * @description     Production configuration with security and environment variables
 * @file            templates/webapp/app.conf.prod.tmpl
 * @version         0.7.0
 * @release         2025-09-14
 * @repository      https://github.com/peterthoeny/jpulse-framework
 * @author          Site Administrator
 * @copyright       2025 Site Administrator
 * @license         AGPL v3, see LICENSE file
 * @genai           95%, Cursor 1.2, Claude Sonnet 4
 */

// Production deployment configuration
// Generated by jpulse-setup for production deployment
{
    app: {
        name: '%SITE_NAME%',
        shortName: '%SITE_SHORT_NAME%',
        siteId: '%SITE_ID%',
        domainName: '%DOMAIN_NAME%'
    },

    deployment: {
        mode: 'prod',
        prod: {
            pm2Instances: %PM2_INSTANCES%
        }
    },

    middleware: {
        session: {
            secret: '%ENV:SESSION_SECRET%',
            cookie: {
                secure: true,  // HTTPS required in production
                maxAge: 3600000,  // 1 hour
                httpOnly: true,
                sameSite: 'strict'
            }
        }
    },

    database: {
        mode: 'standalone',
        standalone: {
            options: {
                authSource: '%DB%',
                auth: {
                    username: '%ENV:DB_USER%',
                    password: '%ENV:DB_PASS%'
                }
            }
        }
    }
}

// EOF webapp/app.conf.prod.tmpl
